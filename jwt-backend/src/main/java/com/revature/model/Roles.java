package com.revature.model;

/**
 * Ensure that you follow the same nomenclature ROLE_XXX since 
 * Spring security uses ROLE_ prefix by default when using 
 * hasRole() method.
 */
public enum Roles {
	ROLE_USER, ROLE_ADMIN
}
